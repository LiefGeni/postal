# Excel 总表合并工具

主要解决邮政数据问题。

一个本地运行的网页工具，用来上传多个 Excel 文件，把指定工作表的数据合并成一个总表，并导出为 `.xlsx`。

## 本地运行

```bash
npm install
npm run dev
```

然后打开终端里显示的本地地址。

## 功能

- 批量上传 `.xlsx`、`.xls`、`.csv`
- 为每个文件选择工作表
- 预览前几行数据
- 设置表头行
- 自动合并列名
- 导出合并后的总表

所有处理都在浏览器本地完成，不上传文件到服务器。
